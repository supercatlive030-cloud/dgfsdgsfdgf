# Update Notification

## Maintenance mode enabled (Donation + Sponsor)
**What changed:**
- `donation.html` is now temporarily unavailable.
- `sponsor.html` is now temporarily unavailable.

**User-facing message (exact):**
- `unfortantly this page is down for matements`

**Navigation added:**
- Both pages now include a **“Go to Home”** button linking to `home.html`.

