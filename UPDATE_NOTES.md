# Update Notification

## Donation page re-opened
**What changed:**
- `donation.html` has been rebuilt and re-opened.
- The page now includes a donation amount selector (presets + custom), a donor name/email form, and a "💜 Donate" button.
- The donate button is wired to a configurable `DONATION_LINK` constant in the inline script. Set `DONATION_LINK` to the real payment URL (e.g. PayPal, Ko-fi, Buy Me a Coffee) when ready.

## Maintenance mode enabled (Sponsor only)
**What changed:**
- `sponsor.html` is now temporarily unavailable.

**Navigation added:**
- The sponsor page includes a **"Go to Home"** button linking to `home.html`.
